$(function () {
    // slick start

    $('.slick-slider-main').slick({
        dots: true,
        infinite: true,
        speed: 500,
        fade: true,
        cssEase: 'linear',
        prevArrow: ".prev",
        nextArrow: ".next"

      });







})